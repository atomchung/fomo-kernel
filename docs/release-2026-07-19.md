# 2026-07-19 promotion release checklist

Goal: ship a version on Sunday that reliably completes the workflow and produces a card.

## P0 acceptance

- [x] `SKILL.md` is a thin entry point and route-specific flows load on demand.
- [x] `prepare`, `preview`, `finalize`, `resume`, and `repair-projections` expose a stable CLI.
- [x] Code validates required motive questions and the evidence gate.
- [x] `new_evidence` creates an append-only thesis decision event.
- [x] Canonical sessions commit through an atomic staging-directory rename; identical retries are no-ops and conflicts fail closed.
- [x] A deterministic renderer creates private Markdown/HTML and public Markdown.
- [x] Cross-client adapters declare their real control/rendering capabilities, use a fixed question fallback, and record generated-versus-presented card events without storing trade content.
- [x] Public cards exclude amounts, dates, tickers, exact weights, and agent-authored free text.
- [x] Mixed-market cards render TW against ^TWII and US against SPY without synthesizing a total alpha.
- [x] A positive benchmark excess renders the engine-owned allocation-versus-selection split.
- [x] Public cards retain fixed behavior-pattern copy plus engine-owned beta and benchmark-excess percentage points.
- [x] English and Traditional Chinese render the same engine facts.
- [x] ETF allocation and concentration policy affects sizing, diversification, decision exits, and what-if analysis.
- [x] Missing ETF metadata enters the honesty ledger instead of defaulting to zero.
- [x] Legacy JSONL/card projections are rebuildable, and data export/reset covers v2 directories.
- [x] Developer documentation and skill instructions are English-only, enforced by a regression test.
- [x] The complete offline test suite passes.

## Promotion demo

1. Run a Traditional Chinese test drive: `python3 engine/review.py prepare --test-drive --language zh-TW`. Read `review_plan.state_root` from the output and pass `--root <state_root>` to every later command in this demo — the test drive is isolated from real coach memory.
2. Prepare the deterministic add-thesis fixture, select `new_evidence` without a source, and show that preview rejects it. The built-in test-drive persona exercises the headline-motive branch instead.
3. Add a claim and source, preview again, and let the user select one rule.
4. Finalize and deliver the card following `skills/fomo-kernel/references/card-delivery.md`: show `card-private.md`, confirm `card-private.html` exists and its `<!-- WIDGET-FRAGMENT-START -->` block renders on a graphical surface (or falls back to verbatim Markdown), and show `card-public.md` from the same session. Verify the privacy-safe UX receipt so artifact generation cannot stand in for user-visible delivery.
5. Repeat in English and emphasize that localization does not change the analysis contract.
6. Compare a broad-market ETF with a thematic ETF to demonstrate concentration policy.
7. Dogfood a second review against the same persistent local root with a later full-history input. Confirm that the private card opens by reconciling the prior rule, presents due exit checks with frozen-price swap framing, historical exit backlog, recurring problem memory, market context, and any horizon contradiction without flooding the question queue, and still converges on at most one new rule.

## Sunday go or no-go

Go when `python3 tests/run_all.py` passes, one anonymized publishable CSV completes end to end in both locales, a second review against the same local state visibly reconciles the prior rule and all weekly-memory surfaces above, and a human confirms that the public card contains no private data.

Do not ship if any required question can be skipped, finalization can leave partial canonical state, the public card leaks a ticker/amount/date, a broad allocation ETF is treated as single-stock concentration, or a projection failure requires questioning the user again.

## Machine preflight — 2026-07-17

- [x] `python3 tests/run_all.py`: all fourteen offline suites passed; the v2 session/E2E suite passed thirty-nine tests.
- [x] `TR_TEST_NETWORK=1 python3 tests/test_sample_styles.py`: all twenty-three persona tests passed with no network skips, including the TW/US mixed-market path.
- [x] CLI `prepare -> resume -> preview -> finalize -> repair-projections` rehearsals completed in English and Traditional Chinese for both the built-in test drive and synthetic `sample_tw_mixed.csv`; all four produced private/public cards with no projection error and live market context.
- [x] The mixed-market private cards showed TW/^TWII and US/SPY separately with the winning-market split; public cards showed market labels, beta, and benchmark-excess percentage points without benchmark or holding tickers.
- [x] The deterministic evidence-gate, ETF-policy, same-root due-revisit lifecycle, weekly-memory renderer, and public private-token regressions passed.
- [ ] Run one anonymized publishable real CSV end to end in both locales. The repository contains synthetic fixtures only, so no fixture was represented as real dogfood.
- [ ] Run the owner second review against the same real local state and inspect all weekly-memory surfaces together. Automated tests cover the components, but they do not replace this manual dogfood pass.
- [ ] Complete the final human privacy and publishable-quality review of `card-public.md`.

## Implemented after the P0 gate scope was frozen

- [x] Initial snapshot onboarding now accepts a normalized position JSON envelope through `review.py`. A coding agent may transcribe a table or screenshot locally and keep the temporary JSON outside the repository, but the engine does not perform OCR or upload the source, and the agent does not calculate weights or assemble card/state artifacts.
- [x] Snapshot reviews are opening portfolio checks limited to supported structural facts, with inferred thesis initialization and a later transaction-history upgrade path. A complete initial snapshot may establish an accounting anchor; an incomplete snapshot produces a review but is not an anchor. Later transactions may unlock history while ledger-derived current holdings remain canonical and unreconciled current-view claims fail closed.
- This addition does not expand the 2026-07-19 go/no-go checklist. The remaining manual gates above stay focused on the already selected real-CSV and second-review flows; snapshot dogfood is a separate follow-up validation.

## Cross-client UX preflight — 2026-07-19

- [x] `tests/test_interaction_trajectory.py` proves native controls, the fixed text fallback, widget delivery, Markdown degradation, weekly-memory presentation, and intentional generated-without-presented failures.
- [x] `python3 tests/run_all.py`: all seventeen offline suites passed after the #230 adapter contract and receipt were added; no engine numeric logic changed.
- [ ] Complete `tests/agent/manual-cross-client-ux.md` in Codex and one comparison host over isolated same-input roots, including a returning weekly review and owner verdicts. Automated receipts prove trajectory shape, not that the owner found the controls, card, or memory useful.

## Explicitly deferred to P1

- Second and subsequent snapshot reconciliation: compare the declared snapshot with ledger-derived holdings, show a narrow diff, and write an explicit adjustment/new anchor without guessing the cause.
- Post-release lens research and possible multi-lens selection; current v2 does not load the rubric assets as a runtime persona.
- Automatic expense-ratio and tracking-error enrichment from a live ETF data source. P0 uses a local instrument map and explicit missing-data disclosure.
- Automated generation and publishing of GTM assets. This release includes synchronized English and Traditional Chinese README content, localized product copy, and a shareable public card.
