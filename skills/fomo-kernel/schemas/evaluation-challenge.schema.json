{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fomo-kernel.local/schemas/evaluation-challenge.schema.json",
  "title": "Fomo Kernel Evaluation Challenge (issue #479, Wave B cut 2)",
  "description": "The `challenge` block engine/review.py's cmd_consider emits beside the trade-evaluation row it stores: what THIS specific answer must put in front of THIS specific user -- which facts, in the user's own words which sentences, which of their own rules this trade collides with, which limitations the numbers carry, and what the engine did not look at. The readable contract; skills/fomo-kernel/engine/evaluation_challenge.py::build_challenge holds the enforcement, the same 'no jsonschema dependency' posture every other schema file in this directory already states about its own hand-rolled producer. Computed, never stored: cmd_consider returns this block alongside the stored row (`{\"evaluation\": row, \"challenge\": challenge, ...}`) but never writes it onto a trade-evaluation.schema.json row in trade_evaluations.jsonl, and never folds it into review.py's _evaluation_id seed. Two reasons, both already paid for elsewhere in this codebase. It is a pure function of premise/basis/consequence/rule_collisions/context, every one of which the row already freezes -- storing it would be a derived duplicate able to disagree with its own inputs, and no reader needs a historical version (`--resolve` copies frozen fields forward without consulting it, and reconciliation reads dates and quantities; writing a field nothing reads back is issue #429's own failure mode). And the evaluation_id seed identifies the subject being evaluated -- this trade, this book, this stated reason -- not a presentation obligation about it; because this block is derived entirely from already-seeded inputs, adding it would not even change which calls converge to the same id, it would only move every existing one. See evaluation_challenge.py's own module docstring, section 'Emitted, not stored', for the complete reasoning.",
  "type": "object",
  "additionalProperties": false,
  "required": ["must_state", "rule_effects", "quote_verbatim", "unchecked", "case_required", "required_coverage"],
  "properties": {
    "must_state": {
      "type": "array",
      "description": "Ordered owed facts this answer must put in front of the user -- a floor on content, never a script for wording (SKILL.md rule 8: 'Brevity bounds what an answer produces, never which facts it owes'). Facts, not sentences: several entries commonly belong in one clause of prose (the basis entries read as one clause -- 'computed on your recorded book as of the 20th, nine days old' -- not four bullet points). Presentation order mirrors evaluation_challenge.TOPICS: basis first, because every number that follows is measured against that book, then price_basis, because those numbers are measured at a market session too, then position, concentration, cash, rule_collision, disclosure, and excluded_holding, the trailing topics qualifying the numbers stated earlier. Deliberately a SUPERSET of required_coverage, and the gap is intentional rather than an oversight: position, concentration, cash, and excluded_holding facts are always owed here in prose but never required to be cited by an anchored claim in --agent-case; and within rule_collision, this array also names a collision whose state is unjudged or unmapped -- silently omitting a rule the engine could not evaluate would read as a clean rule, so it must be named -- while required_coverage excludes exactly those two states, because a case claim can only truthfully cite a fact the engine actually judged, and forcing one for every unmeasurable rule would pad the case to satisfy a checker rather than to inform the user (the 'eval must not pin current wording' failure mode this repository has already shipped once).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["topic", "value"],
        "description": "One owed fact.",
        "properties": {
          "topic": {
            "enum": ["basis", "price_basis", "position", "concentration", "cash", "rule_collision", "disclosure", "excluded_holding"],
            "description": "Which family of fact this is. basis: which book answered, how current it was, and its exact identity -- unconditional, because a weight quoted without the book it was measured on is a number about an unknown denominator. price_basis: which market session that book was valued at (#618), present only when the basis carries price observations and never manufactured for an unpriced answer -- basis.as_of is the last row of the RECORD (the last trade or the snapshot anchor) and says nothing about when the market was observed, while since #611 every weight below is a share of a current close, so the same premise re-asked returns different numbers with no attributable cause unless the answer says which close. The frame summary (basis.price_observations.as_of) is stated unconditionally and a per-instrument entry is added only where that instrument's own session differs from it: naming every ticker on a same-day frame would pad the floor with one entry per holding saying the same thing, while a frame date alone lets one fresh instrument stand in for a stale one (#583 section 2). position: what this trade does to the position the user actually asked about. Its first entry states the premise's own price only when consequence.premise.price_basis is 'observed' (#777, owner ruling 2026-08-02): the caller stated no premise.price and the engine defaulted it to its own observed close, and that entry's detail.price_basis is the literal 'observed' so the fact is not confused with the weight entries beside it -- silent when the caller stated their own price, since that is the ordinary case and states nothing new. Its before-weight entry appears only when the ticker is already held, since a weight of zero for a name the user does not own is arithmetic rather than a fact worth a sentence. concentration: book concentration and driver overlap after the trade; ai_pct/max_sector_pct are omitted rather than stated as a false zero on a book with no sector/AI classification at all -- the separate unclassified_book disclosure covers that case, and unmapped_driver covers the narrower one where it is the premise's own ticker that cannot be classified. A PARTIALLY classified book is the case this omission does not reach: max_sector is present, so both readings are stated as measurements, and they are -- of the classified part of the book only. unclassified_book is what says so, and names the weight it was measured without (#598) -- and the two boolean triggers are stated only when true, never as an explicit false. cash: what the trade leaves in cash; whether that balance can be trusted rides the disclosure topic instead of being folded in here. rule_collision: every one of the user's own rules this trade has any effect on, keyed on consequence.RULE_EFFECTS rather than on the book's absolute state (#579), plus every rule that could not be judged at all (unjudged/unmapped) -- compliant is the only effect that may pass in silence. The state vocabulary silenced resolved_existing_breach too, because clear cannot say whether the line was crossed before this trade. disclosure: every limitation the engine attached to these numbers, addressed by the same index a required_coverage disclosure entry and an --agent-case anchor would use. excluded_holding: which positions sat outside the denominator every percentage above was measured against, whether because they could not be valued (#515) or because the ledger's integrity record names them (#673) -- partial_book (a disclosure entry) says THAT something was excluded; this says WHAT, and `detail.reason` says why, which is what tells the two apart."
          },
          "value": {
            "type": ["string", "number", "boolean"],
            "description": "The fact itself, typed according to topic: an identity or date string for most of basis, or an integer day count for basis.stale_days; a weight or dollar number for position, cash, and most of concentration -- including position's own optional leading entry, the premise's price itself, present only when it was defaulted (#777); a rule-effect string (consequence.RULE_EFFECTS, minus compliant) for rule_collision -- the transaction's effect, never the book's absolute state, which is what an owner-live walk was told when a sell that reduced an over-cap position from 80% to 75% was described as breaking the rule (#579); a row recorded before rule_effect existed falls back to its state string; a disclosure-key string for disclosure; a ticker string for excluded_holding. A boolean appears only for the two concentration triggers (oversize_triggered/concentration_triggered), and only when true -- a false trigger is the absence of a fact and is never emitted as an explicit false."
          },
          "anchor": {
            "type": "string",
            "minLength": 1,
            "pattern": "^(basis|consequence|rule_collisions)\\.",
            "description": "Optional: a dot-separated path into the frozen {basis, consequence, rule_collisions} bundle (answer_provenance.build_record) that a --agent-case engine_fact claim may cite to reproduce this exact value. Present only when it actually resolves -- every anchor this module emits has already been walked against the frozen record with answer_provenance.resolve_anchor before being offered, so an anchor the challenge hands out is always one validate_agent_case will accept; the surface that tells the agent how to cite a fact and the gate that judges the citation agree by construction, not by two authors keeping the same rule in mind. Absent whenever a well-formed path does not resolve, not only when a fact is unaddressable in principle: some real tickers contain a dot (2330.TW), so a path such as consequence.after.weights.2330.TW walks as weights -> '2330' -> 'TW' and resolves to nothing, and a rule_collision row with no rule_id (the schema allows this defensively; problems.py never produces one in practice) cannot be addressed by anchor either, since rule_collisions is re-keyed by rule_id. In both cases the fact is still owed and still stated in `value`; it simply cannot be cited by path, which is why anchor is optional on an entry rather than the entry itself being dropped when its path does not resolve."
          },
          "detail": {
            "type": "object",
            "additionalProperties": false,
            "description": "Optional: topic-specific payload riding alongside a rule_collision, excluded_holding, price_basis, or position entry only; no other topic populates this key. A rule_collision entry carries rule_id/text/state/worsens -- the rule's own text rides along verbatim so the answer can quote what the user wrote rather than paraphrase it, and state/worsens ride along as MACHINE DIAGNOSTICS: they are what a QA run or a replay reconciles against, and since #579 they are deliberately no longer the fact the answer is told to state (that is `value`, the transaction effect). An excluded_holding entry carries reason, why that holding could not be valued and folded into the denominator. A per-instrument price_basis entry carries ticker, which is the only place the instrument's identity survives when its anchor cannot be built. A position entry carries price_basis (#777), present only on the one entry stating the premise's own defaulted price -- distinguishing it from the weight entries the same topic also carries, none of which populate detail at all.",
            "properties": {
              "ticker": {
                "type": "string",
                "minLength": 1,
                "description": "Which instrument this price_basis entry dates. Present on a per-instrument price_basis entry only. Load-bearing rather than redundant with the anchor: a ticker whose own name contains a dot (2330.TW) cannot be addressed by a dotted path, so the entry keeps its date and loses its anchor -- and without this key the answer would carry a market session with no instrument attached to it."
              },
              "rule_id": {
                "type": ["string", "null"],
                "description": "The colliding rule's own identity, mirroring trade-evaluation.schema.json's rule_collisions[].rule_id. null on the defensively-allowed rule row with no id -- the same row whose must_state entry above carries no anchor."
              },
              "text": {
                "type": ["string", "null"],
                "description": "The rule's own text, verbatim, as stored on rules.jsonl -- present so the answer can quote it exactly rather than paraphrase it."
              },
              "state": {
                "enum": ["would_breach", "already_over", "clear", "unjudged", "unmapped"],
                "description": "The book's absolute collision state, mirroring consequence.COLLISION_STATES and trade-evaluation.schema.json's rule_collisions[].state. Diagnostic only since #579: it answers where the book stands, never what this transaction does, and reading it as the transaction's verdict is the defect that failed owner-live acceptance on 2026-08-02."
              },
              "worsens": {
                "type": ["boolean", "null"],
                "description": "Set only when the frozen state is already_over: whether this trade moves the relevant reading in the bad direction. null everywhere else -- a boolean that means nothing outside that one state is worse than an absent one. Diagnostic only since #579, and insufficient on its own even as a direction: a strict > makes \"reduced\" and \"did not move\" the same false."
              },
              "reason": {
                "enum": ["unusable_shares", "unavailable_cost", "integrity_oversell"],
                "description": "Why this holding is outside the usable book, mirroring trade-evaluation.schema.json's consequence.excluded_holdings[].reason exactly (and consequence.EXCLUSION_REASONS, which both mirror). Engine-assigned; never a user or agent claim. `integrity_oversell` says the holding was left out because the ledger recorded a sell with no matching prior buy, not because a valuation fact was missing (#673) -- an answer that reports it as unvalued would send the user after a cost that is already on record."
              },
              "price_basis": {
                "enum": ["observed"],
                "description": "Present only on the one position entry stating the premise's own defaulted price (#777): the caller supplied no premise.price and consequence.validate_premise filled it from the engine's own observed close. The single valid value is 'observed' -- a user-stated price adds no entry here at all, so this key's mere presence already is the signal; it is not emitted carrying any other value."
              }
            }
          }
        }
      }
    },
    "rule_effects": {
      "type": "array",
      "description": "The product-safe projection of what THIS TRANSACTION does to each of the user's own rules (#579 section 3, #713 repair step 4). One entry per rule whose effect is not `compliant`; empty when no rule speaks, and empty for a row recorded before rule_effect existed, which has no deterministic effect to project. Deliberately carries no `state`, no `worsens`, and no retrieval, recovery or validator chronology: the complete machine diagnostics stay on the stored trade-evaluation row, which is what a QA run and a later replay read, and this block is the half a user-facing answer is built from. That separation is the whole of #713 repair step 4 -- a correctness repair that hid diagnostics from maintainers, or a wording repair that left the rule meaning wrong, would each have solved one half at the other's expense.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["rule_id", "text", "effect", "must_convey", "must_not_convey"],
        "description": "What the answer must and must not convey about one rule.",
        "properties": {
          "rule_id": {
            "type": ["string", "null"],
            "description": "The rule's own identity, mirroring trade-evaluation.schema.json's rule_collisions[].rule_id. null on the defensively-allowed row with no id."
          },
          "text": {
            "type": ["string", "null"],
            "description": "The rule's own text, verbatim, as the user wrote it -- present so the answer quotes their line rather than paraphrasing it."
          },
          "effect": {
            "enum": ["new_breach", "worsened_existing_breach", "improved_but_still_over",
                     "resolved_existing_breach", "unchanged_existing_breach",
                     "unjudged", "unmapped"],
            "description": "consequence.RULE_EFFECTS minus compliant, which is the silent non-event. Derived by consequence.classify_rule_effect from the reading before, the reading after and the line -- one deterministic function, so no consumer composes a verdict out of state and worsens. Two of these are not derivable from state/worsens at all: improved_but_still_over and unchanged_existing_breach are both already_over + worsens:false, and resolved_existing_breach is indistinguishable from compliant under state alone."
          },
          "limit": {
            "type": "number",
            "description": "Optional: the line the effect was classified against, as a fraction. Present for every rule with a threshold reading (max_pos_pct and the concentration trio) and absent for avgdown_count, whose reading is a count of events attributable to this trade rather than a level, and for unjudged/unmapped. Here because the correct sentence names the number -- 'reduces the position from 80% to 75%, but it remains above your 20% rule' -- and nothing else in the payload carries it."
          },
          "limit_source": {
            "enum": ["user_cap", "engine_default"],
            "description": "Whose line it is, mirroring consequence.LIMIT_SOURCES. Present exactly when `limit` is. user_cap: the user set this position cap and trade_recap.valid_position_cap accepted it, so the answer may call it their rule. engine_default: this engine picked the threshold, and calling it the user's rule would put the product's own default under their name -- a claim they never made."
          },
          "must_convey": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"enum": ["crossed_by_this_trade", "over_before_this_trade", "moved_toward_the_line", "moved_further_over", "unchanged_by_this_trade", "over_after", "under_after", "not_evaluated", "rule_held"]},
            "description": "Semantic slots the answer must carry for this effect -- meanings, never wording, in the same spirit as must_state being facts rather than sentences. A host may compose them into one clause; it may not drop one. improved_but_still_over carries three, because the correct statement contains both truths at once (the position moved toward the line AND it is still over it) plus the fact that the line was already crossed before this trade."
          },
          "must_not_convey": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"enum": ["crossed_by_this_trade", "over_before_this_trade", "moved_toward_the_line", "moved_further_over", "unchanged_by_this_trade", "over_after", "under_after", "not_evaluated", "rule_held"]},
            "description": "Meanings the answer may not attach to this effect, and the load-bearing half. PR #608 stated the same rule as prose and an improving trade was still described as a breach on the next owner-live walk, so what an answer may NOT say is data here rather than a paragraph to remember. Disjoint from must_convey for every effect."
          }
        }
      }
    },
    "quote_verbatim": {
      "type": "array",
      "uniqueItems": true,
      "description": "The user's own words, to be reproduced in the answer rather than summarized or reworded -- the one part of this answer that may not be paraphrased. Their text is already frozen on the evaluation row's context (decision-context.schema.json); what this block adds is that it is owed BACK to the user inside the answer. Silently rewording the stated reason into the agent's own phrasing would present the agent's reading of the decision as the user's own -- the same substitution answer_provenance.validate_agent_case's case 8 refuses in the other direction, where a public_fact claim may not restate what the user said as if it were an external fact. Empty on a context-free consider call, which captured no prose to quote; the other four keys are still populated in full.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["field", "text"],
        "description": "One sentence the user is on record as having said, and where it came from.",
        "properties": {
          "field": {
            "type": "string",
            "pattern": "^(reason|why_now|evidence_refs\\[[0-9]+\\])$",
            "description": "Which decision-context field this text came from: 'reason', 'why_now', or 'evidence_refs[<index>]' for the index-th cited evidence reference, in the order the caller supplied them on --decision-context."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "The exact string as the user supplied it, unmodified -- not summarized, not translated, not corrected."
          }
        }
      }
    },
    "unchecked": {
      "type": "array",
      "minItems": 4,
      "uniqueItems": true,
      "description": "What this call did not look at, named rather than left silent. Deliberately disjoint from must_state's disclosure topic: a disclosure is a gap in a number the engine DID compute, while an unchecked item is a risk it never went near at all -- folding the two together would let 'the weights are on cost' and 'nobody looked at liquidity' read as the same kind of caveat, and the second is the one silence turns into a false clean bill of health. liquidity, valuation, tax, and position_fit are unconditional and always present (hence this array's minimum length of four), lifted verbatim from references/trade-consequence.md's own sentence: 'Liquidity, valuation, tax consequences, and whether the position still fits this person are all real risks the engine does not measure.' evidence_delta is present only when a decision context was supplied on this call: the engine cannot mechanically tell new information apart from a price move that only feels like new information, so it names that judgment call as unsettled rather than leaving the agent's read of it unlabelled. evidence_refs_unverified is present only when the user actually cited an evidence reference: the engine records that a reference was cited, but it does not fetch, date, or believe it.",
      "items": {
        "enum": ["liquidity", "valuation", "tax", "position_fit", "evidence_delta", "evidence_refs_unverified"]
      }
    },
    "case_required": {
      "type": "object",
      "additionalProperties": false,
      "required": ["for", "against"],
      "description": "The floor a two-sided --agent-case submission must clear, restated here so this block is self-contained for a reader holding only this payload -- the same number said where the agent is reading, mirroring evaluation_challenge.CASE_REQUIRED. This block only names the floor; the refusal of a submission that does not clear it is enforced by answer_provenance.validate_agent_case (which rejects a present-but-empty side) and by answer-provenance.schema.json's own minItems:1 on `for`/`against`, not by this schema. Currently one claim minimum on each side.",
      "properties": {
        "for": {"type": "integer", "minimum": 1, "description": "Minimum number of claims the case FOR the trade must carry."},
        "against": {"type": "integer", "minimum": 1, "description": "Minimum number of claims the case AGAINST the trade must carry."}
      }
    },
    "required_coverage": {
      "type": "array",
      "description": "The mechanically enforced SUBSET of must_state: exactly what a --agent-case submission is refused for leaving uncited. Imported from answer_provenance.required_coverage, the same function validate_agent_case calls internally before a row is ever written, so the facts this block tells the agent it owes and the facts the gate actually refuses on are one list, read twice, and cannot drift apart. Only four of must_state's eight topics ever contribute an entry here -- basis (at most one entry, covering whichever of stale_days/completeness triggered it), price_basis (at most one entry, present exactly when the basis carries price observations, and never for an unpriced answer), rule_collision (only the would_breach/already_over states, never unjudged/unmapped -- see must_state's own description for why those two are named but not required), and disclosure (every disclosure that was stated is also required, the one topic where the two arrays name exactly the same facts). position, concentration, cash, and excluded_holding facts are always owed in must_state's prose but never appear here: requiring an --agent-case claim for every cash balance or every excluded ticker would inflate the case to satisfy a checker rather than to inform the user (the 'eval must not pin current wording' failure mode this repository has already shipped once). Empty when nothing on this evaluation must be covered -- a fresh, non-stale, fully declared book with no rule_collision in a covered state and no disclosures at all.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "owes", "key"],
        "description": "One obligation an --agent-case submission must cover with at least one anchored engine_fact claim.",
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Matched by PREFIX, not by exact equality alone: covered when some engine_fact claim's own anchor equals this path, or sits under it (anchor == path, or anchor starts with path + '.'). For owes:basis (path is always the bare string 'basis'), owes:price_basis (path is always 'basis.price_observations') and owes:rule_collision (path is 'rule_collisions.<rule_id>'), this path names a container that never itself resolves through answer_provenance.resolve_anchor -- the sub-fact an engine_fact claim actually anchors at sits one level further in, at basis.stale_days or basis.completeness, at basis.price_observations.as_of or .by_ticker.<ticker>, or at rule_collisions.<rule_id>.rule_effect, .state or .worsens, so only the startswith branch of the match can ever satisfy these three. For owes:disclosure the path (consequence.disclosures.<index>) already IS the exact resolvable anchor the matching must_state entry carries -- one scalar fact with nothing beneath it to prefix-match against, so only the exact-equality branch can satisfy it. WHERE TWO PATHS NEST, one claim pays the narrower one only: 'basis.price_observations' sits under 'basis', so a claim citing the price day discharges the price-day obligation and leaves the staleness obligation open (answer_provenance._paid_path). Prefix matching alone would have let a single price-day citation silently cover both."
          },
          "owes": {
            "enum": ["disclosure", "basis", "price_basis", "rule_collision"],
            "description": "Which family of obligation this is, mirroring must_state's topic vocabulary but never its full width. disclosure: one of consequence.disclosures must be cited by anchor. basis: the book's own staleness or incompleteness must be cited by some basis.* anchor, once, regardless of which basis field the citing claim names -- except one under basis.price_observations, which pays the narrower obligation below instead. price_basis: which market session valued this book must be cited by a basis.price_observations.* anchor (#618). Present only when the basis carries price observations at all, so an unpriced answer owes nothing here and there is no obligation to cite a date that does not exist. rule_collision: a rule this trade creates, worsens, improves-but-does-not-clear, or leaves unchanged above the line must be cited by a rule_collisions.<id>.rule_effect, .state or .worsens anchor -- never an unjudged or unmapped one, which must_state still names in prose but which required_coverage deliberately excludes, and never a resolved_existing_breach one, which the answer states as good news but which no missing claim can hide a risk behind."
          },
          "key": {
            "enum": ["cost_basis", "cash_unreliable", "unmapped_driver",
                     "unclassified_book", "etf_not_decomposed", "partial_book",
                     "cash_anchor_unmatched",
                     "stale", "unverified", "stale_and_unverified",
                     "price_observed",
                     "new_breach", "worsened_existing_breach",
                     "improved_but_still_over", "unchanged_existing_breach",
                     "would_breach", "already_over"],
            "description": "Which specific instance within the owes family. Mirrors consequence.DISCLOSURES exactly when owes is disclosure; one of answer_provenance.py's own three literals when owes is basis (stale: only stale_days > 0; unverified: only basis.completeness != declared_complete; stale_and_unverified: both at once -- and a basis entry never fires for any other reason, so exactly one of these three appears whenever owes is basis); always the single literal price_observed when owes is price_basis, which has exactly one instance per evaluation and needs no discriminator -- the date itself is the fact and belongs in must_state's value, not restated here where a closed enum could not hold it; the colliding rule_collisions[].rule_effect when owes is rule_collision, which by construction is always one of the four answer_provenance._COVERED_EFFECTS here (#579); the trailing would_breach/already_over pair is the pre-#579 fallback a row recorded before rule_effect existed still emits, kept so an old row stays readable rather than silently owing nothing."
          }
        }
      }
    }
  }
}
